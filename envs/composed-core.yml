name: composed-core
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy>=1.24
  - scipy
  - astropy
  - matplotlib
  - pandas
  - tqdm
  - pytest
  - ipykernel
  - jupyterlab
  - emcee
  - corner
  - pip:
      # Install CompoSED itself from the repository root with:
      # python -m pip install -e ".[dev,plot,samplers,notebooks]"
      - pocomc
