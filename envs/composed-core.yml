name: composed-core
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy>=1.24
  - astropy
  # Install CompoSED and only the extras needed for the analysis:
  # python -m pip install -e ".[dev,plot]"
  # python -m pip install -e ".[samplers]"  # optional
