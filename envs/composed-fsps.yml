name: composed-fsps
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy=1.26
  - astropy
  - pip:
      # Python binding only.  FSPS data files and SPS_HOME still follow the
      # upstream python-fsps/FSPS install instructions.
      - fsps==0.4.7
      - astro-sedpy==0.4.1
