# Data Containers

```{admonition} Stability
:class: warning
`SEDDataset` is the stable photometric container. `SpectrumDataset` and
`SpectroPhotometricDataset` are experimental and may change as the spectral
likelihood gains instrumental and covariance support.
```

```{automodule} composed.data
:members: SEDDataset, SpectrumDataset, SpectroPhotometricDataset
:show-inheritance:
```
